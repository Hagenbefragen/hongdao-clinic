---
description: The complete Uniquehorn meta-workflow — Ahnung senses → Strategy filters → Funnel orchestrates → Quality gates → Ship Engine executes. End-to-end autonomous company.
---

# /fullOrchestra — The Autonomous Company Engine

// turbo-all

## Skills Chain

`quantum_intuition` → `strategic_advisor` → `funnel_orchestrator` → `quality_gateway` → `ship_engine`

## Philosophy

> "Every skill is an instrument. Alone they're talented. Together, they're a masterpiece."

This is the **Meta-Workflow**. It chains the 5 layer-spanning skills into a single autonomous pipeline. Use this when you want the FULL OHM Symphony — from sensing to shipping.

## Step 1: `quantum_intuition` — SENSE

1. Skill runs automatically (always-on)
2. GUT (X1-X4): Context classification → mode detection
3. HEART (X5-X8): Resonance matching → skill surfacing
4. CROWN (X9-X12): Purpose alignment → mission filter
5. Output: Ranked skill recommendations with φ-weights

## Step 2: `strategic_advisor` — FILTER

1. Read SKILL.md: `c:\ohm\.agent\skills\strategic_advisor\SKILL.md`
2. Apply the RICE framework (Reach × Impact × Confidence / Effort)
3. Check competitive position against horizon scan
4. Apply NI framework alignment check
5. Decision: Build / Defer / Kill

### 🚫 STRATEGY GATE

- RICE < 3 → **DEFER. Not the right time.**
- Mission misalignment → **KILL. Doesn't serve OHM.**

## Step 3: `funnel_orchestrator` — ORCHESTRATE

1. Read SKILL.md: `c:\ohm\.agent\skills\funnel_orchestrator\SKILL.md`
2. Chain the 10-stage CRM pipeline
3. Invoke supporting skills per stage (content, pitch, conversion)
4. Hand off between stages automatically
5. Track completion through all 10 stages

## Step 4: `quality_gateway` — GATE

1. Read SKILL.md: `c:\ohm\.agent\skills\quality_gateway\SKILL.md`
2. Check every output for:
   - Consistency (contradicts existing docs?)
   - Plausibility (improvements realistic?)
   - OHM Alignment (sovereignty, privacy, edge-first?)
   - Transparency (simulated vs real clearly labeled?)
   - Persuasiveness (skeptical CTO accepts?)
3. Score: 5 gates, need 4/5 PASS minimum

### 🚫 QUALITY GATE

- <3/5 PASS → **REWORK. Route back to originating step.**

## Step 5: `ship_engine` — SHIP

1. Read SKILL.md: `c:\ohm\.agent\skills\ship_engine\SKILL.md`
2. Phase 1: Process Description → User signs off
3. Phase 2: Autonomous Implementation
4. Phase 3: Self-Heal (fix own bugs)
5. Phase 4: UI/UX Polish
6. Phase 5: Validation Report

## Output

```markdown
## Full Orchestra Report

- Ahnung: [context + recommended skills + φ-weights]
- Strategy: [RICE score + decision]
- Funnel: [10-stage completion status]
- Quality: [5-gate scores]
- Ship: [implementation + validation]
- PDCA: [what to improve next run]
```
